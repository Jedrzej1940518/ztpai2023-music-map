
endpoints:
get - genres
get - music_festivals/[genre]/[from-to]
get - concerts/[genre]/[from-to]

get - music_festival/[id]

post  - login

/*  
  "generate-models": "sequelize-auto -o './models' -d $POSTGRES_DB -h $DB_HOST -u $POSTGRES_USER -p $POSTGRES_PORT -x $POSTGRES_PASSWORD -e postgres"
*/